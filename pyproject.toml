[project]
name = "yllehs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "denismaggior8", email = "denis.maggiorotto@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.14.3",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pyyaml>=6.0.3",
    "quickjs>=1.19.4",
    "requests>=2.34.2",
]

[build-system]
requires = ["uv_build>=0.11.9,<0.12.0"]
build-backend = "uv_build"
